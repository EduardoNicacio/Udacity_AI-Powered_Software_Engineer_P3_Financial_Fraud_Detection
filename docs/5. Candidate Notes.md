# Candidate Notes

Based on my comprehensive research and analysis, I've developed a complete architecture plan for the high-volume, low-latency financial fraud detection system. Here's the detailed plan:

## Financial Fraud Detection System - Architecture Plan

### Executive Summary

This architecture replaces the aging batch-driven system with a cloud-native, event-driven solution on Microsoft Azure using Kubernetes for elastic scaling. It processes 1,000+ TPS with sub-100ms classification latency, provides real-time alerting, and ensures compliance through complete audit trails.

---

## 1. Core System Requirements Analysis

### Functional Requirements

- **Real-time Transaction Analysis**: Process and classify transactions within <100ms
- **Compliance Replay**: Store raw transactions and ML scores for audit
- **Peak Throughput**: Support 1,000 transactions per second
- **Elastic Scaling**: Auto-scale based on load without peak costs
- **Real-time Alerting**: Frontend dashboard with instant notifications
- **Observability**: Comprehensive monitoring, logging, and tracing

### Non-Functional Requirements

- **Latency**: <100ms for ML classification (p99)
- **Availability**: 99.95% uptime
- **Durability**: Zero data loss for transactions
- **Security**: PCI-DSS compliance, encryption
- **Auditability**: Complete audit trail for regulatory compliance

---

## 2. Microservices Architecture Design

### Solution Architecture Diagram

```mermaid
graph TB
    subgraph "External Systems"
        Bank[Bank Transaction Systems]
        Analysts[Fraud Analysts]
    end

    subgraph "API Layer"
        APIM[Azure API Management<br/>Rate Limiting & Auth]
    end

    subgraph "Microservices - AKS Cluster"
        TIS[Transaction Ingestion<br/>Service]
        FDS[Fraud Detection<br/>Service]
        ANS[Alert Notification<br/>Service]
        CSS[Compliance Store<br/>Service]
    end

    subgraph "Data & Messaging"
        EH[Azure Event Hubs<br/>Message Broker]
        Redis[Azure Cache for Redis<br/>Feature Store]
        CosmosDB[(Cosmos DB<br/>Compliance Store)]
        AML[Azure ML<br/>Model Serving]
    end

    subgraph "Frontend"
        Dashboard[React Dashboard<br/>Real-time Alerts]
        SignalR[Azure SignalR<br/>WebSocket Service]
    end

    subgraph "Observability"
        AppInsights[Application Insights<br/>Distributed Tracing]
        Prometheus[Prometheus<br/>Metrics]
        LogAnalytics[Log Analytics<br/>Centralized Logs]
    end

    Bank -->|REST/gRPC| APIM
    APIM --> TIS
    TIS -->|TransactionReceived| EH
    EH --> FDS
    EH --> CSS
    FDS -->|Query Features| Redis
    FDS -->|ML Inference| AML
    FDS -->|TransactionClassified| EH
    FDS -->|FraudDetected| EH
    EH --> ANS
    ANS -->|Push Alerts| SignalR
    SignalR --> Dashboard
    CSS -->|Store Records| CosmosDB
    Analysts --> Dashboard

    TIS --> AppInsights
    FDS --> AppInsights
    ANS --> AppInsights
    CSS --> AppInsights
    TIS --> Prometheus
    FDS --> Prometheus
    ANS --> Prometheus
```

### Service Decomposition

**Service 1: Transaction Ingestion Service**
- **Responsibility**: Accept transactions, validate, enrich, publish to event stream
- **Inputs**: REST/gRPC API calls
- **Outputs**: TransactionReceived events to Event Hubs
- **Scaling**: HPA based on request rate

**Service 2: Fraud Detection Service**
- **Responsibility**: Consume transactions, extract features, run ML inference, classify
- **Inputs**: TransactionReceived events
- **Outputs**: TransactionClassified events
- **Scaling**: HPA based on queue depth

**Service 3: Alert Notification Service**
- **Responsibility**: Process fraud alerts, notify analysts via frontend
- **Inputs**: FraudDetected events
- **Outputs**: Alert notifications via WebSocket/SSE
- **Scaling**: HPA based on alert volume

**Service 4: Compliance Store Service**
- **Responsibility**: Store transactions and ML scores for replay/audit
- **Inputs**: TransactionReceived and TransactionClassified events
- **Outputs**: Stored records in Cosmos DB
- **Scaling**: Cosmos DB autoscaling

### Inter-Service Communication

- **Synchronous**: REST/gRPC for transaction submission, WebSocket for real-time updates
- **Asynchronous**: Event-driven via Azure Event Hubs for decoupled, scalable messaging

---

## 3. Event-Driven Architecture & Message Schemas

### Event Flow Diagram

```mermaid
sequenceDiagram
    participant Bank as Bank Systems
    participant TIS as Transaction Ingestion
    participant EH as Event Hubs
    participant FDS as Fraud Detection
    participant Redis as Feature Store
    participant AML as Azure ML
    participant ANS as Alert Notification
    participant CSS as Compliance Store
    participant Dashboard as Fraud Dashboard

    Bank->>TIS: POST /transactions
    TIS->>TIS: Validate & Enrich
    TIS->>EH: Publish TransactionReceived
    TIS-->>Bank: 202 Accepted

    par Parallel Processing
        EH->>FDS: Consume TransactionReceived
        EH->>CSS: Consume TransactionReceived
    end

    FDS->>Redis: Query Features
    Redis-->>FDS: Return Features
    FDS->>AML: Run ML Inference
    AML-->>FDS: Return Fraud Score

    alt Fraud Score > 0.8
        FDS->>EH: Publish FraudDetected
        FDS->>EH: Publish TransactionClassified
    else Fraud Score <= 0.8
        FDS->>EH: Publish TransactionClassified
    end

    EH->>ANS: Consume FraudDetected
    ANS->>Dashboard: Push Alert (WebSocket)
    Dashboard-->>Analyst: Display Alert

    EH->>CSS: Consume TransactionClassified
    CSS->>CSS: Store in Cosmos DB
```

### Event Types and Publishers/Consumers

| Event Type | Publisher | Consumers | Purpose |
| ------------ | ----------- | ----------- | --------- |
| `TransactionReceived` | Transaction Ingestion | Fraud Detection, Compliance Store | New transaction available |
| `TransactionClassified` | Fraud Detection | Alert Notification, Compliance Store | ML classification complete |
| `FraudDetected` | Fraud Detection | Alert Notification | High-confidence fraud detected |
| `AlertCreated` | Alert Notification | Fraud Analyst Dashboard | New alert for analyst |
| `AlertAcknowledged` | Fraud Analyst Dashboard | Alert Notification | Analyst acknowledges alert |
| `AlertResolved` | Fraud Analyst Dashboard | Alert Notification | Investigation completed |

### Event Schemas (Avro/JSON)

**TransactionReceived Event:**

```json
{
  "eventId": "uuid-v4",
  "eventType": "TransactionReceived",
  "timestamp": "ISO-8601",
  "data": {
    "transactionId": "string",
    "accountId": "string",
    "amount": "decimal",
    "currency": "string",
    "merchantId": "string",
    "transactionType": "enum[DEBIT|CREDIT|TRANSFER]",
    "location": {"latitude": "decimal", "longitude": "decimal"},
    "deviceId": "string",
    "channel": "enum[MOBILE|WEB|ATM|BRANCH]"
  }
}
```

**TransactionClassified Event:**

```json
{
  "eventId": "uuid-v4",
  "eventType": "TransactionClassified",
  "data": {
    "transactionId": "string",
    "fraudScore": "decimal[0.0-1.0]",
    "decision": "enum[APPROVE|DECLINE|REVIEW]",
    "modelVersion": "string",
    "features": {
      "velocityScore": "decimal",
      "locationScore": "decimal",
      "amountScore": "decimal"
    },
    "processingTimeMs": "integer"
  }
}
```

**FraudDetected Event:**

```json
{
  "eventId": "uuid-v4",
  "eventType": "FraudDetected",
  "data": {
    "transactionId": "string",
    "accountId": "string",
    "fraudScore": "decimal",
    "riskLevel": "enum[LOW|MEDIUM|HIGH|CRITICAL]",
    "fraudType": "enum[IDENTITY_THEFT|CARD_FRAUD|ACCOUNT_TAKEOVER|OTHER]",
    "recommendedAction": "enum[HOLD_ACCOUNT|BLOCK_CARD|REVIEW|NOTIFY_CUSTOMER]"
  }
}
```

---

## 4. End-to-End Transaction Flow

### Transaction Processing Flow Diagram

```mermaid
flowchart TD
    Start([Transaction Arrives]) --> Validate{Validate<br/>Transaction}
    Validate -->|Invalid| Reject[Reject Transaction]
    Validate -->|Valid| Enrich[Enrich Transaction<br/>Add Metadata]

    Enrich --> Publish[Publish to<br/>Event Hubs]
    Publish --> FanOut{Fan-out<br/>Parallel Processing}

    FanOut --> FraudDetection[Fraud Detection<br/>Service]
    FanOut --> ComplianceStore[Compliance Store<br/>Service]

    FraudDetection --> ExtractFeatures[Extract Features<br/>from Redis]
    ExtractFeatures --> RunML[Run ML<br/>Inference]
    RunML --> ScoreCheck{Fraud Score<br/>> 0.8?}

    ScoreCheck -->|Yes| FraudAlert[Publish<br/>FraudDetected Event]
    ScoreCheck -->|No| Classify[Publish<br/>TransactionClassified Event]

    FraudAlert --> NotifyAnalyst[Notify Analyst<br/>via SignalR]
    Classify --> StoreResult[Store Result<br/>in Cosmos DB]

    ComplianceStore --> StoreTransaction[Store Raw<br/>Transaction]

    NotifyAnalyst --> Dashboard[Display Alert<br/>on Dashboard]
    StoreResult --> AuditLog[Compliance<br/>Audit Log]
    StoreTransaction --> AuditLog

    Dashboard --> AnalystAction{Analyst<br/>Action}
    AnalystAction -->|Approve| ApproveTransaction[Approve Transaction]
    AnalystAction -->|Block| BlockTransaction[Block Transaction]
    AnalystAction -->|Investigate| Investigate[Create Investigation]

    ApproveTransaction --> End([Transaction Complete])
    BlockTransaction --> End
    Investigate --> End
```

---

## 5. Frontend Design for Fraud Analysts

### Frontend Architecture Diagram

```mermaid
graph TB
    subgraph "React SPA Dashboard"
        subgraph "Components"
            AlertFeed[Real-time<br/>Alert Feed]
            AlertQueue[Alert Queue<br/>Management]
            TxDetail[Transaction<br/>Detail View]
            Analytics[Analytics<br/>Dashboard]
        end

        subgraph "State Management"
            ReactQuery[React Query<br/>Server State]
            Context[React Context<br/>App State]
        end

        subgraph "Real-time Layer"
            SignalRClient[SignalR<br/>Client]
            WebSocket[WebSocket<br/>Connection]
        end
    end

    subgraph "Backend Services"
        API[REST API<br/>Endpoints]
        SignalRServer[Azure SignalR<br/>Service]
    end

    AlertFeed --> SignalRClient
    SignalRClient --> WebSocket
    WebSocket --> SignalRServer

    AlertQueue --> API
    TxDetail --> API
    Analytics --> API

    API -->|HTTP/REST| Backend[Backend Services]
    SignalRServer -->|WebSocket| AlertNotif[Alert Notification<br/>Service]

    subgraph "Performance Optimizations"
        VirtualScroll[Virtual Scrolling<br/>Large Lists]
        LazyLoad[Lazy Loading<br/>Components]
        Cache[Client-side Caching<br/>React Query]
    end

    AlertFeed --> VirtualScroll
    TxDetail --> LazyLoad
    API --> Cache
```

### Key Features

1. **Real-time Alert Feed**: WebSocket connection, color-coded alerts, sound notifications
2. **Alert Queue Management**: Filterable/sortable list, assignment workflows, status tracking
3. **Transaction Detail View**: Complete history, ML model explanation, geographic visualization
4. **Performance Optimizations**: Virtual scrolling, connection pooling, lazy loading

---

## 6. Observability Strategy

### Observability Architecture Diagram

```mermaid
graph TB
    subgraph "Application Layer"
        TIS[Transaction<br/>Ingestion]
        FDS[Fraud<br/>Detection]
        ANS[Alert<br/>Notification]
        CSS[Compliance<br/>Store]
    end

    subgraph "Instrumentation"
        OTel[OpenTelemetry<br/>SDK]
        AutoInstrument[Auto-instrumentation<br/>Agents]
    end

    subgraph "Collection"
        OTelCollector[OTel Collector<br/>Gateway]
        Prometheus[Prometheus<br/>Metrics Server]
    end

    subgraph "Storage & Analysis"
        AppInsights[Application<br/>Insights]
        LogAnalytics[Log Analytics<br/>Workspace]
        MetricsAzure[Azure Monitor<br/>Metrics]
    end

    subgraph "Visualization & Alerting"
        Grafana[Grafana<br/>Dashboards]
        AzureDashboards[Azure<br/>Dashboards]
        AlertManager[Alert Manager<br/>PagerDuty/Teams]
    end

    subgraph "Three Pillars"
        Metrics[Metrics<br/>Prometheus]
        Traces[Traces<br/>Application Insights]
        Logs[Logs<br/>Log Analytics]
    end

    TIS --> OTel
    FDS --> OTel
    ANS --> OTel
    CSS --> OTel
    TIS --> AutoInstrument
    FDS --> AutoInstrument

    OTel --> OTelCollector
    AutoInstrument --> OTelCollector

    OTelCollector --> Metrics
    OTelCollector --> Traces
    OTelCollector --> Logs

    Metrics --> Prometheus
    Traces --> AppInsights
    Logs --> LogAnalytics

    Prometheus --> Grafana
    AppInsights --> AzureDashboards
    LogAnalytics --> Grafana

    Grafana --> AlertManager
    AzureDashboards --> AlertManager
```

### Three Pillars of Observability

#### Metrics Collection (Azure Monitor + Prometheus)

**Key Metrics:**

1. **Transaction Processing Latency**: p99 < 100ms (alert: >150ms for 5 min)
2. **Fraud Detection Accuracy**: >95% true positive rate (alert: <90% for 1 hour)
3. **System Throughput**: Handle 1,000 TPS (alert: <800 TPS)
4. **Error Rates**: <0.1% (alert: >1% for 5 min)
5. **ML Model Inference Latency**: p99 < 50ms (alert: >75ms for 5 min)

#### Distributed Tracing (Azure Application Insights + OpenTelemetry)

- End-to-end tracing from ingestion to alert generation
- Correlation IDs propagated across all services
- Custom spans for ML inference and feature extraction
- Sampling: 100% for errors, 10% for successful transactions

#### Centralized Logging (Azure Log Analytics + Serilog)

- Structured JSON logging across all services
- Correlation IDs for request tracing
- Retention: 90 days for compliance, 30 days for operational

### Trace Correlation Diagram

```mermaid
graph LR
    subgraph "Transaction Processing Trace"
        A[Transaction Received<br/>traceId: abc-123<br/>spanId: span-001] --> B[Validate Transaction<br/>traceId: abc-123<br/>spanId: span-002]
        B --> C[Extract Features<br/>traceId: abc-123<br/>spanId: span-003]
        C --> D[ML Inference<br/>traceId: abc-123<br/>spanId: span-004]
        D --> E[Publish Classification<br/>traceId: abc-123<br/>spanId: span-005]
    end

    subgraph "Log Correlation"
        F[Log Entry<br/>traceId: abc-123<br/>spanId: span-004<br/>message: ML inference completed<br/>duration: 45ms]
    end

    subgraph "Metric Correlation"
        G[Histogram Bucket<br/>transaction_duration_ms: 45<br/>exemplar: traceId: abc-123]
    end

    D -.->|Links to| F
    D -.->|Links to| G
```

### SLOs (Service Level Objectives)

| SLI | SLO | Measurement | Alert Threshold |
| ----- | ----- | ------------- | ----------------- |
| Transaction Processing Latency | p99 < 100ms | 5-minute window | p99 > 150ms for 5 min |
| System Availability | 99.95% uptime | 30-day window | <99.9% |
| Fraud Detection Accuracy | >95% TPR | Daily | <90% for 1 hour |
| Alert Notification Latency | <1 second | Per-alert | >2 seconds for 5% |
| Data Durability | Zero loss | Continuous | Any loss event |

### Correlation Strategy

- **Trace-Log**: Every log includes `traceId` and `spanId`
- **Metric-Trace**: Exemplars link high-latency metrics to specific traces

---

## 7. Technology Stack

### Core Technologies

| Component | Technology | Justification |
| ----------- | ------------ | --------------- |
| Container Orchestration | Azure Kubernetes Service (AKS) | Managed K8s with auto-scaling, enterprise security |
| Message Broker | Azure Event Hubs | High-throughput, low-latency streaming (1M+ TPS) |
| API Gateway | Azure API Management | Rate limiting, auth, request routing |
| ML Model Serving | Azure ML + ONNX Runtime | Real-time inference with <50ms latency |
| Feature Store | Azure Cache for Redis | Sub-millisecond feature retrieval |
| Compliance Database | Azure Cosmos DB | Global distribution, automatic indexing |
| Real-time Updates | Azure SignalR | Managed WebSocket service |
| Frontend | React + TypeScript | Component-based UI with strong typing |
| Observability | OpenTelemetry + Azure Monitor | Vendor-neutral instrumentation |
| Logging | Serilog + Azure Log Analytics | Structured logging with powerful queries |

### ML Model Stack

- **Training**: Azure ML + Scikit-learn/XGBoost
- **Format**: ONNX for cross-platform optimization
- **Serving**: Azure ML Managed Endpoints with auto-scaling
- **Feature Engineering**: Apache Spark on Azure Synapse

### Technology Stack Diagram

```mermaid
graph TB
    subgraph "Presentation Layer"
        React[React + TypeScript]
        SignalR[Azure SignalR]
    end

    subgraph "API Layer"
        APIM[Azure API Management]
        gRPC[gRPC Services]
    end

    subgraph "Application Layer - AKS"
        TIS[Transaction Ingestion]
        FDS[Fraud Detection]
        ANS[Alert Notification]
        CSS[Compliance Store]
    end

    subgraph "Data Layer"
        EventHubs[Azure Event Hubs]
        Redis[Azure Cache for Redis]
        CosmosDB[(Cosmos DB)]
        BlobStorage[Azure Blob Storage]
    end

    subgraph "ML Layer"
        AzureML[Azure ML]
        ONNX[ONNX Runtime]
        FeatureStore[Feature Store]
    end

    subgraph "Observability Layer"
        OTel[OpenTelemetry]
        AppInsights[Application Insights]
        Prometheus[Prometheus]
        Grafana[Grafana]
    end

    React --> SignalR
    React --> APIM
    APIM --> gRPC
    gRPC --> TIS

    TIS --> EventHubs
    EventHubs --> FDS
    EventHubs --> CSS

    FDS --> Redis
    FDS --> AzureML
    AzureML --> ONNX
    FDS --> FeatureStore

    CSS --> CosmosDB
    ANS --> SignalR

    TIS --> OTel
    FDS --> OTel
    ANS --> OTel
    OTel --> AppInsights
    OTel --> Prometheus
    Prometheus --> Grafana
```

---

## 8. Scalability & Resilience

### Scaling Architecture Diagram

```mermaid
graph TB
    subgraph "Auto-scaling Triggers"
        CPU["CPU Usage > 70%"]
        Memory["Memory Usage > 80%"]
        QueueDepth["Event Hub Queue Depth"]
        RequestRate["Request Rate > 800 TPS"]
    end

    subgraph "Scaling Controllers"
        HPA["Horizontal Pod Autoscaler"]
        KEDA["KEDA Event-driven Scaling"]
        ClusterAutoscaler["Cluster Autoscaler"]
    end

    subgraph "AKS Cluster"
        subgraph "Node Pool 1 - General"
            Node1["Node 1"]
            Node2["Node 2"]
        end
        subgraph "Node Pool 2 - Compute Optimized"
            Node3["Node 3 ML Inference"]
            Node4["Node 4 ML Inference"]
        end
        subgraph "Node Pool 3 - Memory Optimized"
            Node5["Node 5 Feature Store"]
        end
    end

    subgraph "Services"
        TIS["Transaction Ingestion Pods: 2-10"]
        FDS["Fraud Detection Pods: 3-15"]
        ANS["Alert Notification Pods: 2-8"]
    end

    CPU --> HPA
    Memory --> HPA
    QueueDepth --> KEDA
    RequestRate --> KEDA

    HPA --> TIS
    HPA --> ANS
    KEDA --> FDS

    TIS --> Node1
    TIS --> Node2
    FDS --> Node3
    FDS --> Node4
    ANS --> Node1

    ClusterAutoscaler --> NodePool1
    ClusterAutoscaler --> NodePool2
    ClusterAutoscaler --> NodePool3
```

### Scaling Strategies

- **Microservices**: HPA based on CPU/memory and custom metrics, KEDA for Event Hub consumers
- **Analytic Workloads**: Azure Synapse Analytics for batch features, Cosmos DB autoscaling
- **Event Messaging**: Event Hubs auto-inflate, partition-based parallelism

### Resilience Patterns

```mermaid
graph TB
    subgraph "Circuit Breaker Pattern"
        Request[Incoming Request] --> Circuit{Circuit<br/>State}
        Circuit -->|Closed| ServiceCall[Call Service]
        Circuit -->|Open| Fallback[Fallback<br/>Logic]
        Circuit -->|Half-Open| Probe[Probe<br/>Request]

        ServiceCall -->|Success| SuccessResponse[Return Response]
        ServiceCall -->|Failure| FailureCount[Increment<br/>Failure Count]
        FailureCount -->|Threshold Reached| CircuitOpen[Open Circuit]

        Fallback --> CachedResponse[Return Cached<br/>Response]
        Fallback --> DefaultResponse[Return Default<br/>Response]

        Probe -->|Success| CircuitClose[Close Circuit]
        Probe -->|Failure| CircuitOpen
    end

    subgraph "Retry with Exponential Backoff"
        InitialRequest[Initial Request] --> Retry{Retry<br/>Attempt}
        Retry -->|Attempt 1| Wait1[Wait 100ms]
        Retry -->|Attempt 2| Wait2[Wait 200ms]
        Retry -->|Attempt 3| Wait3[Wait 400ms]
        Wait1 --> RetryCall1[Retry Call]
        Wait2 --> RetryCall2[Retry Call]
        Wait3 --> RetryCall3[Retry Call]
    end

    subgraph "Bulkhead Pattern"
        MainThread[Main Thread Pool] --> IsolatedPool1[Isolated Pool 1<br/>Transaction Processing]
        MainThread --> IsolatedPool2[Isolated Pool 2<br/>ML Inference]
        MainThread --> IsolatedPool3[Isolated Pool 3<br/>Alert Processing]
    end
```

- **Circuit Breaker**: For ML model calls with rule-based fallback
- **Retry with Exponential Backoff**: All inter-service communication
- **Bulkhead Pattern**: Isolated thread pools for different operations

### Single Points of Failure Mitigation

| Component | SPOF Risk | Mitigation |
| ----------- | ----------- | ------------ |
| Event Hubs | Medium | Geo-redundant, auto-inflate |
| Cosmos DB | Low | Multi-region writes, automatic failover |
| AKS Cluster | Low | Multi-zone deployment |
| ML Model Service | Medium | Multi-region, fallback rules |

---

## 9. Trade-offs & Challenges

### Key Trade-offs

1. **Latency vs. Accuracy**: Optimized for <100ms latency, may sacrifice complex features → Use lightweight models with feature pre-computation
2. **Cost vs. Performance**: Real-time ML inference → Auto-scaling for cost optimization
3. **Consistency vs. Availability**: Eventual consistency for non-critical data → Strong consistency for transactions

### Financial Services Challenges

- **Regulatory Compliance**: PCI-DSS, SOX, GDPR → Encryption, access controls, audit logging
- **Data Privacy**: Customer PII → Data masking, tokenization, RBAC
- **Audit Requirements**: Complete replay → Immutable event store in Cosmos DB

---

## 10. Implementation Roadmap

### Implementation Timeline Diagram

```mermaid
gantt
    title Implementation Roadmap
    dateFormat  YYYY-MM-DD
    axisFormat  %b %Y

    section Phase 1: Foundation
    AKS Cluster Setup           :a1, 2026-01-01, 7d
    Event Hubs Configuration    :a2, after a1, 5d
    Transaction Ingestion Svc   :a3, after a2, 14d
    Basic Observability         :a4, after a1, 10d

    section Phase 2: Core ML Pipeline
    Feature Store (Redis)       :b1, 2026-02-01, 7d
    Fraud Detection Service     :b2, after b1, 14d
    ML Model Training           :b3, after b1, 10d
    End-to-End Flow             :b4, after b2, 7d

    section Phase 3: Alerting & Dashboard
    Alert Notification Service  :c1, 2026-03-01, 10d
    React Dashboard             :c2, after c1, 14d
    Real-time Alert Feed        :c3, after c2, 7d
    Alert Management            :c4, after c3, 7d

    section Phase 4: Compliance & Observability
    Compliance Store            :d1, 2026-04-01, 10d
    Full Observability Stack    :d2, after d1, 10d
    SLO Monitoring              :d3, after d2, 7d
    Audit & Replay              :d4, after d3, 7d

    section Phase 5: Optimization
    Performance Testing         :e1, 2026-05-01, 10d
    Security Hardening          :e2, after e1, 7d
    DR Testing                  :e3, after e2, 7d
    Documentation               :e4, after e3, 7d
```

### Phase 1: Foundation (Weeks 1-4)
- AKS cluster and networking setup
- Event Hubs configuration
- Transaction Ingestion Service implementation
- Basic observability setup

### Phase 2: Core ML Pipeline (Weeks 5-8)
- Feature Store (Redis) deployment
- Fraud Detection Service implementation
- ML model training and deployment
- End-to-end transaction flow

### Phase 3: Alerting & Dashboard (Weeks 9-12)
- Alert Notification Service implementation
- React dashboard with SignalR
- Real-time alert feed
- Alert management workflows

### Phase 4: Compliance & Observability (Weeks 13-16)
- Compliance Store implementation
- Full observability stack
- SLO monitoring and alerting
- Audit and replay capabilities

### Phase 5: Optimization & Hardening (Weeks 17-20)
- Performance testing and optimization
- Security hardening
- Disaster recovery testing
- Documentation and runbooks

---

## 11. Conclusion

This architecture provides a scalable, resilient, and observable fraud detection system that meets all requirements:

✅ **Real-time processing**: <100ms classification latency  
✅ **Elastic scaling**: AKS with HPA/KEDA for cost optimization  
✅ **Compliance**: Complete audit trail with replay capability  
✅ **Real-time alerting**: SignalR-powered dashboard for analysts  
✅ **Observability**: Comprehensive metrics, traces, and logs with SLOs  
✅ **Peak throughput**: 1,000+ TPS with Event Hubs  
✅ **Resilience**: Circuit breakers, retries, and multi-region deployment

The event-driven design ensures loose coupling and independent scaling, while the comprehensive observability stack provides visibility into system health and performance for operational excellence.
